pipeline {
    agent any
    parameters {
            string(defaultValue: "Maintainer", description: 'Enter User Role')
    }
    stages {
        stage('listVals')
            steps {
                echo "user's role = ${params.userRole}"
            }

        }

    }
}